# Kopyalayın: cp deploy/env.example .env  (proje kökünden)
# .env Git'e eklenmez. Üretimde güçlü değerler kullanın.

# Oturum imzası — en az 32 karakter rastgele metin
SESSION_SECRET=degistirin-uzun-rastgele-bir-metin

# HTTPS üretimde: true — Sadece http://localhost denemesi: false
COOKIE_SECURE=false

# İlk kurulumda DB boşsa oluşacak admin şifresi
ADMIN_PASSWORD=admin123

# Postgres — db ve app aynı değeri kullanır (ilk init sonrası değiştirmek volume gerektirir)
POSTGRES_PASSWORD=muhasebe

# Host portu (konteyner içi her zaman 3000). Sunucuda 3006 — Nginx örneği ile uyumlu
APP_PORT=3006
